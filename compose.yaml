services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: chemagent-backend
    restart: unless-stopped
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-}
      SERPER_API_KEY: ${SERPER_API_KEY:-}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost,http://127.0.0.1}
      # Tell glibc to aggressively return freed memory back to the OS.
      # Without this, Python's allocator holds onto large heaps (RDKit / OpenBabel
      # import spikes) even after the objects are GC'd, which exhausts host RAM.
      MALLOC_TRIM_THRESHOLD_: "65536"
      PYTHONMALLOC: malloc
    ports:
      - "3030:3030"
    deploy:
      resources:
        limits:
          memory: 2.5g     # hard ceiling — OOM kill stays inside this container
        reservations:
          memory: 1g       # guaranteed slice; kernel won't evict below this
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:3030/health')"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 40s   # RDKit + OpenBabel cold-import can take 20-30 s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-}
    container_name: chemagent-frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    environment:
      NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-}
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 196m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s

  gateway:
    image: nginx:1.27-alpine
    container_name: chemagent-gateway
    restart: unless-stopped
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    deploy:
      resources:
        limits:
          memory: 96m
        reservations:
          memory: 32m