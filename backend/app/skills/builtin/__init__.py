"""Built-in skill manifests shipped with ChemAgent.

Each sub-package provides a ``MANIFEST: SkillManifest`` object and optionally
a ``prompts/`` directory with skill-specific system-prompt fragments.

Current skills:
  rdkit_analysis/  — descriptor computation, similarity, scaffold analysis
  mol_3d/         — 3-D conformer generation, PDBQT preparation
"""
