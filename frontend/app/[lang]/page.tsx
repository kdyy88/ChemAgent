/**
 * Locale-aware home page.
 * This is a thin re-export of the existing Home component — all rendering
 * logic stays in app/page.tsx. The [lang] segment is handled by the layout.
 */
export { default } from '../page'
